"""RAG-based value stream selection pipeline.

The canonical implementation lives in the rag-summary/ directory
(which is its own git repo). This package re-exports key entry points.

rag-summary has a hyphen so it cannot be imported directly as a Python package.
Use importlib or sys.path manipulation if needed.
"""
